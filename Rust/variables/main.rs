fn main () {
    let mut x = 5; //Basic declaration of a variable
    let mut y: u32 = 5; //Declaration of a variable with specific type
    println!("The result of x + y is: {}",  x + y);
    let tup = (500,6.4,1);
    println! ("{}",tup.1);
}
